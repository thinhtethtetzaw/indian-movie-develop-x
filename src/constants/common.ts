export enum AUTH_STAGES {
  UNAUTHENTICATED = "unauthenticated",
  AUTHENTICATED = "authenticated",
}

export enum ROLES {
  ADMIN = "admin",
  AUDITOR = "auditor",
}

export enum LANGUAGES_API_HEADER {
  en = "en",
  zh_cn = "cn",
  zh_tw = "tw",
  ko = "ko",
  ja = "ja",
}
